<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "sql-map-2.dtd" >
<sqlMap namespace="is.objManagement">

	<select id="getObjManagementList" parameterClass="LinkMap" resultClass="LinkMap" remapResults="true">		
		SELECT distinct T.PA_BEN_MANAGE_SEQ,
			   GET_DEPT_NAME(D.DEPTNO,'zh') DEPTNAME,
		       T.PERSON_ID,
		       E.EMPID,
		       E.LOCAL_NAME  CHINESENAME, <!-- 员工名字-->
		       GET_GLOBAL_NAME(P.SEXCODE, #interLanguage:VARCHAR#) SEX_NAME,
      		    GET_GLOBAL_NAME(P.REG_TYPE_CODE,'zh') AS REG_TYPE_CODE_NAME,<!-- 户口性质-->
       		   C2.CONTENT AS STATUS_CODE_NAME,
		      CHANGER_IDNO(P.IDCARD_NO) IDCARD_NO,
		       TO_CHAR(T.CREATE_DATE,'YYYYMM') AS JOIN_MONTH,
		       to_char(E.DATE_STARTED,'yyyy-mm-dd') AS DATE_STARTED,
		       to_char(E.DATE_LEFT,'yyyy-mm-dd') AS DATE_LEFT,
		       CASE
		         WHEN T.SOCIAL_STATUS = 'JOIN' THEN
		          '新参保'
		         WHEN T.SOCIAL_STATUS = 'STOP' THEN
		          '停保'
		         WHEN T.SOCIAL_STATUS = 'LAST' THEN
		          '在保'
		       END SOCIAL_STATUS,
		       CHANGER_IDNO(T.SOCIAL_NO) SOCIAL_NO,
		       T.START_DATE,
		       T.END_DATE,
		       T.JOIN_VALUE,
		       T.AVG_SALARY,
		      T.ENDOWMENT_BASE,
		      T.MEDICARE_BASE,
		       T.SHENGYU_BASE,
		       T.COMPO_BASE,
		       T.UNEMP_BASE,
		       T.SERIOUS_BASE,
		       T.BASELINE,
		       T.DATE_TYPE,
       		   T.ERROR_REMARK
		  FROM HR_EMPLOYEE      E,
		       PA_BEN_MANAGE    T,
		       HR_PERSONAL_INFO P,
		       HR_DEPARTMENT    D,
		       SY_GLOBAL_NAME C1,
		       SY_GLOBAL_NAME C2
		 WHERE E.PERSON_ID = T.PERSON_ID
		   AND E.PERSON_ID= P.PERSON_ID(+)
		   AND E.DEPTNO = D.DEPTNO
		   AND P.SEXCODE = C1.NO(+)
		   AND E.STATUS_CODE = C2.NO(+)
		   and C1.LANGUAGE='zh'
		   and C2.LANGUAGE='zh'
		   and E.cpny_id= #companyID:VARCHAR#
		<isNotEmpty prepend="AND" property="yearMonth">
				TO_CHAR(M.CREATE_DATE,'YYYYMM') = #yearMonth#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="startYearMonth">
				T.START_DATE = #startYearMonth#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="endYearMonth">
				T.END_DATE = #endYearMonth#
		</isNotEmpty>
		<isNotEmpty property="socialStatus">
			<isEqual compareValue="FUCK" prepend="AND" property="socialStatus">
					T.SOCIAL_STATUS IN ('JOIN','LAST')
			</isEqual>
			<isNotEqual compareValue="FUCK" prepend="AND" property="socialStatus">
					T.SOCIAL_STATUS = #socialStatus#
			</isNotEqual> 
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="deptno">
				E.deptno in (select t.deptno
							from hr_department t
							start with t.deptno =#deptno# connect by prior t.deptno=t.parent_dept_id)
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="postCoefCode">
				E.POST_COEF = #postCoefCode#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="empStatus">
				E.STATUS_CODE = #empStatus#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="regTypeCode">
				P.REG_TYPE_CODE = #regTypeCode#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="empID">
				T.PERSON_ID = #empID#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="empName">
				E.CHINESENAME LIKE '%' || #empName# || '%'
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="cardID">
				P.IDCARD_NO = #cardID#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="socialNo">
				T.SOCIAL_NO = #socialNo#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="socialNoNull">
				T.SOCIAL_NO IS NULL
		</isNotEmpty>
		$order$
	</select>

	<select id="getObjManagementCnt" parameterClass="linkMap" resultClass="int">
	SELECT distinct count(*)
	 FROM HR_EMPLOYEE      E,
		       PA_BEN_MANAGE    T,
		       HR_PERSONAL_INFO P,
		       HR_DEPARTMENT    D,
		       SY_GLOBAL_NAME C1,
		       SY_GLOBAL_NAME C2
		 WHERE E.PERSON_ID = T.PERSON_ID
		   AND E.PERSON_ID= P.PERSON_ID(+)
		   AND E.DEPTNO = D.DEPTNO
		   AND P.SEXCODE = C1.NO(+)
		   AND E.STATUS_CODE = C2.NO(+)
		   and c1.LANGUAGE='zh'
		and c2.LANGUAGE='zh'
		   AND E.cpny_id= 'C02'
		<isNotEmpty prepend="AND" property="yearMonth">
				TO_CHAR(M.CREATE_DATE,'YYYYMM') = #yearMonth#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="startYearMonth">
				T.START_DATE = #startYearMonth#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="endYearMonth">
				T.END_DATE = #endYearMonth#
		</isNotEmpty>
		<isNotEmpty property="socialStatus">
			<isEqual compareValue="FUCK" prepend="AND" property="socialStatus">
					T.SOCIAL_STATUS IN ('JOIN','LAST')
			</isEqual>
			<isNotEqual compareValue="FUCK" prepend="AND" property="socialStatus">
					T.SOCIAL_STATUS = #socialStatus#
			</isNotEqual> 
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="deptno">
				E.deptno in (select t.deptno
							from hr_department t
							start with t.deptno =#deptno# connect by prior t.deptno=t.parent_dept_id)
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="postCoefCode">
				E.POST_COEF = #postCoefCode#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="empStatus">
				E.STATUS_CODE = #empStatus#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="regTypeCode">
				P.REG_TYPE_CODE = #regTypeCode#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="empID">
				T.PERSON_ID = #empID#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="empName">
				E.CHINESENAME LIKE '%' || #empName# || '%'
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="cardID">
				P.IDCARD_NO = #cardID#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="socialNo">
				T.SOCIAL_NO = #socialNo#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="socialNoNull">
				T.SOCIAL_NO IS NULL
		</isNotEmpty>
		$order$
	</select>
	<update id="allowPaBenObjInsureUpdate" parameterClass="string">
		<![CDATA[
			   UPDATE PA_BEN_MANAGE
			      SET ALLOW_UPDATE = 'Y'
				WHERE PA_BEN_MANAGE_SEQ = #id#
			  ]]>
	</update>
	
	<select id="getAllowPaBenObjInsureUpdate" parameterClass="linkMap"
		resultClass="linkMap" remapResults="true" >
		SELECT distinct T.PA_BEN_MANAGE_SEQ,
			   GET_DEPT_NAME(D.DEPTNO,'zh') DEPTNAME,
		       T.PERSON_ID,
		       E.EMPID,
		       E.LOCAL_NAME  CHINESENAME, <!-- 员工名字-->
		       GET_GLOBAL_NAME(P.SEXCODE, #interLanguage:VARCHAR#) SEX_NAME,
      		    GET_GLOBAL_NAME(P.REG_TYPE_CODE,'zh') AS REG_TYPE_CODE_NAME,<!-- 户口性质-->
       		   C2.CONTENT AS STATUS_CODE_NAME,
		      CHANGER_IDNO(P.IDCARD_NO) IDCARD_NO,
		       TO_CHAR(T.CREATE_DATE,'YYYYMM') AS JOIN_MONTH,
		       to_char(E.DATE_STARTED,'yyyy-mm-dd') AS DATE_STARTED,
		       to_char(E.DATE_LEFT,'yyyy-mm-dd') AS DATE_LEFT,
		       CASE
		         WHEN T.SOCIAL_STATUS = 'JOIN' THEN
		          '新参保'
		         WHEN T.SOCIAL_STATUS = 'STOP' THEN
		          '停保'
		         WHEN T.SOCIAL_STATUS = 'LAST' THEN
		          '在保'
		       END SOCIAL_STATUS,
		       CHANGER_IDNO(T.SOCIAL_NO) SOCIAL_NO,
		       T.START_DATE,
		       T.END_DATE,
		       T.JOIN_VALUE,
		       T.AVG_SALARY,
		      T.ENDOWMENT_BASE,
		      T.MEDICARE_BASE,
		       T.SHENGYU_BASE,
		       T.COMPO_BASE,
		       T.UNEMP_BASE,
		       T.SERIOUS_BASE,
		       T.BASELINE,
		       T.DATE_TYPE,
       		   T.ERROR_REMARK
		  FROM HR_EMPLOYEE      E,
		       PA_BEN_MANAGE    T,
		       HR_PERSONAL_INFO P,
		       HR_DEPARTMENT    D,
		       SY_GLOBAL_NAME C1,
		       SY_GLOBAL_NAME C2
		 WHERE E.PERSON_ID = T.PERSON_ID
		   AND E.PERSON_ID= P.PERSON_ID(+)
		   AND E.DEPTNO = D.DEPTNO
		   AND P.SEXCODE = C1.NO(+)
		   AND E.STATUS_CODE = C2.NO(+)
		    AND C1.LANGUAGE='zh'
		    AND C2.LANGUAGE='zh'
		    AND E.cpny_id= #companyID:VARCHAR#
		    AND T.ALLOW_UPDATE = 'Y'
			$order$
	</select>
	<select id="getPaManageShowListExport" parameterClass="linkMap"
		resultClass="linkMap" remapResults="true">		
		SELECT M.PA_BEN_MANAGE_SEQ,
			   D.DEPTNAME,
		       M.PERSON_ID,
		       E.CHINESENAME,
		       C.CODE_NAME AS SEXNAME,
      		   C1.CODE_NAME AS REG_TYPE_CODE_NAME,
       		   C2.CODE_NAME AS STATUS_CODE_NAME,
		       P.IDCARD_NO,
		       TO_CHAR(M.CREATE_DATE,'YYYYMM') AS JOIN_MONTH,
		       to_char(E.DATE_STARTED,'yyyy-mm-dd') AS DATE_STARTED1,
			   to_char(E.END_PRACTICE_DATE,'yyyy-mm-dd') AS DATE_STARTED,
		       to_char(E.DATE_LEFT,'yyyy-mm-dd') AS DATE_LEFT,
		       CASE
		         WHEN M.SOCIAL_STATUS = 'JOIN' THEN
		          '新参保'
		         WHEN M.SOCIAL_STATUS = 'STOP' THEN
		          '停保'
		         WHEN M.SOCIAL_STATUS = 'LAST' THEN
		          '在保'
		       END SOCIAL_STATUS,
		       M.SOCIAL_NO,
		       M.START_DATE,
		       M.END_DATE,
		       M.JOIN_VALUE,
		       M.AVG_SALARY,
		       M.ENDOWMENT_BASE,
		       M.MEDICARE_BASE,
		       M.SHENGYU_BASE,
		       M.COMPO_BASE,
		       M.UNEMP_BASE,
		       M.SERIOUS_BASE,
		       M.BASELINE,
		       M.DATE_TYPE,
         	   M.ERROR_REMARK
		  FROM HR_EMPLOYEE      E,
		       PA_BEN_MANAGE    M,
		       HR_PERSONAL_INFO P,
		       HR_DEPARTMENT    D,
		       SY_GLOBAL_NAME C1,
		       SY_GLOBAL_NAME C2,
			   SY_GLOBAL_NAME C3
		 WHERE E.PERSON_ID = M.EMPID
		   AND M.PERSON_ID = P.PERSON_ID(+)
		   AND E.DEPTNO = D.DEPTNO
		   AND P.SEXCODE = C1.NO(+)
		   AND P.REG_TYPE_CODE = C2.NO(+)
		   AND E.STATUS_CODE =C3.NO(+)
		<isNotEmpty prepend="AND" property="yearMonth">
				TO_CHAR(M.CREATE_DATE,'YYYYMM') = #yearMonth#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="startYearMonth">
				M.START_DATE = #startYearMonth#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="endYearMonth">
				M.END_DATE = #endYearMonth#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="socialStatus">
				M.SOCIAL_STATUS = #socialStatus#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="deptno">
				E.deptno in (select t.deptno
							from hr_department t
							start with t.deptno =#deptno# connect by prior t.deptno=t.parent_dept_id)
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="postCoefCode">
				E.POST_COEF = #postCoefCode#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="empStatus">
				E.STATUS_CODE = #empStatus#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="regTypeCode">
				P.REG_TYPE_CODE = #regTypeCode#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="empID">
				M.PERSON_ID = #empID#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="empName">
				E.CHINESENAME LIKE '%' || #empName# || '%'
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="cardID">
				P.IDCARD_NO = #cardID#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="socialNo">
				M.SOCIAL_NO = #socialNo#
		</isNotEmpty>
		ORDER BY E.DEPTNO,M.PERSON_ID
	</select>
	
	<update id="backPaBenObjInsureUpdate" parameterClass="string">
		<![CDATA[
			   UPDATE PA_BEN_MANAGE
			      SET ALLOW_UPDATE = ''
			     WHERE ALLOW_UPDATE = 'Y'
			  ]]>
	</update>
	
	<!--==============================================  -->
	
	<update id="updatePaBenManage" parameterClass="LinkMap">
		<![CDATA[
			 UPDATE PA_BEN_MANAGE T
			   SET T.SOCIAL_NO          = #socialNo#,
				   T.JOIN_VALUE         = #joinValue#,
				   T.SOCIAL_STATUS      = #socialStatus#,
			       T.UPDATE_DATE        = SYSDATE,
			       T.UPDATED_BY         = #adminID#,
			       T.ENDOWMENT_BASE =
		             NVL((SELECT CASE
					         WHEN ((SELECT M.AVG_SALARY
					                  FROM PA_BEN_MANAGE M
					                 WHERE M.PA_BEN_MANAGE_SEQ = #seq#) IS NULL) THEN
					          (CASE
					            WHEN #joinValue# > B.UPPER_LIMIT THEN
					             B.UPPER_LIMIT
					            WHEN #joinValue# < B.LOWER_LIMIT THEN
					             B.LOWER_LIMIT
					            ELSE
					             #joinValue#
					          END)
					         WHEN ((SELECT M.AVG_SALARY
					                  FROM PA_BEN_MANAGE M
					                 WHERE M.PA_BEN_MANAGE_SEQ = #seq#) IS NOT NULL) THEN
					          (CASE
					            WHEN (SELECT M.AVG_SALARY
					                    FROM PA_BEN_MANAGE M
					                   WHERE M.PA_BEN_MANAGE_SEQ = #seq#) > B.UPPER_LIMIT THEN
					             B.UPPER_LIMIT
					            WHEN (SELECT M.AVG_SALARY
					                    FROM PA_BEN_MANAGE M
					                   WHERE M.PA_BEN_MANAGE_SEQ = #seq#) < B.LOWER_LIMIT THEN
					             B.LOWER_LIMIT
					            ELSE
					             (SELECT M.AVG_SALARY
					                FROM PA_BEN_MANAGE M
					               WHERE M.PA_BEN_MANAGE_SEQ = #seq#)
					          END)
					       END SSS
					  FROM PA_BEN_BENCHMARK B
					 WHERE B.VERSION_DATE = (SELECT MAX(VERSION_DATE) FROM PA_BEN_BENCHMARK)
					   AND B.INSURANCE_CODE = 'InsuranceTpye100'
					   AND B.REG_TYPE_CODE = (SELECT P.REG_TYPE_CODE
					                            FROM HR_PERSONAL_INFO P
					                           WHERE P.PERSON_ID = #empID#)),0),
		             T.MEDICARE_BASE =
		             NVL((SELECT CASE
					         WHEN ((SELECT M.AVG_SALARY
					                  FROM PA_BEN_MANAGE M
					                 WHERE M.PA_BEN_MANAGE_SEQ = #seq#) IS NULL) THEN
					          (CASE
					            WHEN #joinValue# > B.UPPER_LIMIT THEN
					             B.UPPER_LIMIT
					            WHEN #joinValue# < B.LOWER_LIMIT THEN
					             B.LOWER_LIMIT
					            ELSE
					             #joinValue#
					          END)
					         WHEN ((SELECT M.AVG_SALARY
					                  FROM PA_BEN_MANAGE M
					                 WHERE M.PA_BEN_MANAGE_SEQ = #seq#) IS NOT NULL) THEN
					          (CASE
					            WHEN (SELECT M.AVG_SALARY
					                    FROM PA_BEN_MANAGE M
					                   WHERE M.PA_BEN_MANAGE_SEQ = #seq#) > B.UPPER_LIMIT THEN
					             B.UPPER_LIMIT
					            WHEN (SELECT M.AVG_SALARY
					                    FROM PA_BEN_MANAGE M
					                   WHERE M.PA_BEN_MANAGE_SEQ = #seq#) < B.LOWER_LIMIT THEN
					             B.LOWER_LIMIT
					            ELSE
					             (SELECT M.AVG_SALARY
					                FROM PA_BEN_MANAGE M
					               WHERE M.PA_BEN_MANAGE_SEQ = #seq#)
					          END)
					       END SSS
					  FROM PA_BEN_BENCHMARK B
					 WHERE B.VERSION_DATE = (SELECT MAX(VERSION_DATE) FROM PA_BEN_BENCHMARK)
					   AND B.INSURANCE_CODE = 'InsuranceTpye101'
					   AND B.REG_TYPE_CODE = (SELECT P.REG_TYPE_CODE
					                            FROM HR_PERSONAL_INFO P
					                           WHERE P.PERSON_ID = #empID#)),0),
		             T.SHENGYU_BASE  =
		             NVL((SELECT CASE
					         WHEN ((SELECT M.AVG_SALARY
					                  FROM PA_BEN_MANAGE M
					                 WHERE M.PA_BEN_MANAGE_SEQ = #seq#) IS NULL) THEN
					          (CASE
					            WHEN #joinValue# > B.UPPER_LIMIT THEN
					             B.UPPER_LIMIT
					            WHEN #joinValue# < B.LOWER_LIMIT THEN
					             B.LOWER_LIMIT
					            ELSE
					             #joinValue#
					          END)
					         WHEN ((SELECT M.AVG_SALARY
					                  FROM PA_BEN_MANAGE M
					                 WHERE M.PA_BEN_MANAGE_SEQ = #seq#) IS NOT NULL) THEN
					          (CASE
					            WHEN (SELECT M.AVG_SALARY
					                    FROM PA_BEN_MANAGE M
					                   WHERE M.PA_BEN_MANAGE_SEQ = #seq#) > B.UPPER_LIMIT THEN
					             B.UPPER_LIMIT
					            WHEN (SELECT M.AVG_SALARY
					                    FROM PA_BEN_MANAGE M
					                   WHERE M.PA_BEN_MANAGE_SEQ = #seq#) < B.LOWER_LIMIT THEN
					             B.LOWER_LIMIT
					            ELSE
					             (SELECT M.AVG_SALARY
					                FROM PA_BEN_MANAGE M
					               WHERE M.PA_BEN_MANAGE_SEQ = #seq#)
					          END)
					       END SSS
					  FROM PA_BEN_BENCHMARK B
					 WHERE B.VERSION_DATE = (SELECT MAX(VERSION_DATE) FROM PA_BEN_BENCHMARK)
					   AND B.INSURANCE_CODE = 'InsuranceTpye102'
					   AND B.REG_TYPE_CODE = (SELECT P.REG_TYPE_CODE
					                            FROM HR_PERSONAL_INFO P
					                           WHERE P.PERSON_ID = #empID#)),0),
		             T.COMPO_BASE    =
		             NVL((SELECT CASE
					         WHEN ((SELECT M.AVG_SALARY
					                  FROM PA_BEN_MANAGE M
					                 WHERE M.PA_BEN_MANAGE_SEQ = #seq#) IS NULL) THEN
					          (CASE
					            WHEN #joinValue# > B.UPPER_LIMIT THEN
					             B.UPPER_LIMIT
					            WHEN #joinValue# < B.LOWER_LIMIT THEN
					             B.LOWER_LIMIT
					            ELSE
					             #joinValue#
					          END)
					         WHEN ((SELECT M.AVG_SALARY
					                  FROM PA_BEN_MANAGE M
					                 WHERE M.PA_BEN_MANAGE_SEQ = #seq#) IS NOT NULL) THEN
					          (CASE
					            WHEN (SELECT M.AVG_SALARY
					                    FROM PA_BEN_MANAGE M
					                   WHERE M.PA_BEN_MANAGE_SEQ = #seq#) > B.UPPER_LIMIT THEN
					             B.UPPER_LIMIT
					            WHEN (SELECT M.AVG_SALARY
					                    FROM PA_BEN_MANAGE M
					                   WHERE M.PA_BEN_MANAGE_SEQ = #seq#) < B.LOWER_LIMIT THEN
					             B.LOWER_LIMIT
					            ELSE
					             (SELECT M.AVG_SALARY
					                FROM PA_BEN_MANAGE M
					               WHERE M.PA_BEN_MANAGE_SEQ = #seq#)
					          END)
					       END SSS
					  FROM PA_BEN_BENCHMARK B
					 WHERE B.VERSION_DATE = (SELECT MAX(VERSION_DATE) FROM PA_BEN_BENCHMARK)
					   AND B.INSURANCE_CODE = 'InsuranceTpye103'
					   AND B.REG_TYPE_CODE = (SELECT P.REG_TYPE_CODE
					                            FROM HR_PERSONAL_INFO P
					                           WHERE P.PERSON_ID = #empID#)),0),
		             T.UNEMP_BASE    =
		             NVL((SELECT CASE
					         WHEN ((SELECT M.AVG_SALARY
					                  FROM PA_BEN_MANAGE M
					                 WHERE M.PA_BEN_MANAGE_SEQ = #seq#) IS NULL) THEN
					          (CASE
					            WHEN #joinValue# > B.UPPER_LIMIT THEN
					             B.UPPER_LIMIT
					            WHEN #joinValue# < B.LOWER_LIMIT THEN
					             B.LOWER_LIMIT
					            ELSE
					             #joinValue#
					          END)
					         WHEN ((SELECT M.AVG_SALARY
					                  FROM PA_BEN_MANAGE M
					                 WHERE M.PA_BEN_MANAGE_SEQ = #seq#) IS NOT NULL) THEN
					          (CASE
					            WHEN (SELECT M.AVG_SALARY
					                    FROM PA_BEN_MANAGE M
					                   WHERE M.PA_BEN_MANAGE_SEQ = #seq#) > B.UPPER_LIMIT THEN
					             B.UPPER_LIMIT
					            WHEN (SELECT M.AVG_SALARY
					                    FROM PA_BEN_MANAGE M
					                   WHERE M.PA_BEN_MANAGE_SEQ = #seq#) < B.LOWER_LIMIT THEN
					             B.LOWER_LIMIT
					            ELSE
					             (SELECT M.AVG_SALARY
					                FROM PA_BEN_MANAGE M
					               WHERE M.PA_BEN_MANAGE_SEQ = #seq#)
					          END)
					       END SSS
					  FROM PA_BEN_BENCHMARK B
					 WHERE B.VERSION_DATE = (SELECT MAX(VERSION_DATE) FROM PA_BEN_BENCHMARK)
					   AND B.INSURANCE_CODE = 'InsuranceTpye104'
					   AND B.REG_TYPE_CODE = (SELECT P.REG_TYPE_CODE
					                            FROM HR_PERSONAL_INFO P
					                           WHERE P.PERSON_ID = #empID#)),0),
		             T.BASELINE      =
		             (SELECT CASE
					         WHEN ((SELECT M.AVG_SALARY
					                  FROM PA_BEN_MANAGE M
					                 WHERE M.PA_BEN_MANAGE_SEQ = #seq#) IS NULL) THEN
					          (CASE
					            WHEN #joinValue# > MIN(B.UPPER_LIMIT) THEN
					             'Max'
					            WHEN #joinValue# < MAX(B.LOWER_LIMIT) THEN
					             'Min'
					            ELSE
					             ''
					          END)
					         WHEN ((SELECT M.AVG_SALARY
					                  FROM PA_BEN_MANAGE M
					                 WHERE M.PA_BEN_MANAGE_SEQ = #seq#) IS NOT NULL) THEN
					          (CASE
					            WHEN (SELECT M.AVG_SALARY
					                    FROM PA_BEN_MANAGE M
					                   WHERE M.PA_BEN_MANAGE_SEQ = #seq#) > MIN(B.UPPER_LIMIT) THEN
					             'Max'
					            WHEN (SELECT M.AVG_SALARY
					                    FROM PA_BEN_MANAGE M
					                   WHERE M.PA_BEN_MANAGE_SEQ = #seq#) < MAX(B.LOWER_LIMIT) THEN
					             'Min'
					            ELSE
					             ''
					          END)
					       END SSS
					  FROM PA_BEN_BENCHMARK B
					 WHERE B.VERSION_DATE = (SELECT MAX(VERSION_DATE) FROM PA_BEN_BENCHMARK)
					   AND B.INSURANCE_CODE IN ('InsuranceTpye100',
					                            'InsuranceTpye101',
					                            'InsuranceTpye102',
					                            'InsuranceTpye103',
					                            'InsuranceTpye104')
					   AND B.REG_TYPE_CODE = (SELECT P.REG_TYPE_CODE
					                            FROM HR_PERSONAL_INFO P
					                           WHERE P.PERSON_ID = #empID#)
					 GROUP BY B.VERSION_DATE)
			 WHERE T.PA_BEN_MANAGE_SEQ = #seq#
			  ]]>
	</update>
	
</sqlMap>
